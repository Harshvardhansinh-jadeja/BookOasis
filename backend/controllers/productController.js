// import Product from "@models/product.js";

// export const newProduct = async (req, res) => {
//   //Product Schema takes create function and creaes a new product from the given data from request body.
//   const product = await Product.create(req.body);

//   res.status(200).json({
//     product,
//   });
// };
